import { PromptEditor } from "@/components/prompt-editor";

export const metadata = {
  title: "Nuevo prompt · PromptStore",
};

export default function NuevoPromptPage() {
  return <PromptEditor />;
}
